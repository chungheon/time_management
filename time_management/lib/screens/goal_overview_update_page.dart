import 'package:flutter/material.dart';

class GoalOverviewUpdatePage extends StatelessWidget {
  const GoalOverviewUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Goal Overview Update Page")),
    );
  }
}
